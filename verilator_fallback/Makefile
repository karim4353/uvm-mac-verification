# Simple Makefile to build with Verilator (placeholder)
# TODO: Requires Verilator & cocotb on the host
VERILOG_SRCS = $(wildcard ../rtl/*.sv) ../uvm/tb_top.sv
TOP = tb_top
all:
	@echo "This Makefile is a placeholder. To run with Verilator+cocotb, customize the build rules."
	@echo "See docs/vendor_notes.md and README.md for guidance."
